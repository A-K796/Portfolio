<body>
  <div>
    <h1>映画一覧</h1>
    <table class="table">
      <% @movies.each do |movie| %>
      <tr>
        <td>
          <%= link_to movie_path(movie.id) do %>
          映画画像
          <%= movie.title %>
          <%end%>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</body>